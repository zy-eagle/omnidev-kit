---
description: OmniDev 测试验证与发布文档生成规范，确保交付质量和可部署性。
globs: *
---
# OmniDev Test & Deploy (测试与发布规范)

当 `docs/omnidev-state/02-plan.md` 中的所有开发任务标记为完成（`[x]`）后，自动进入测试与发布阶段。

## 1. 自动化测试验证 (Testing)

### 1.1 测试数据流模拟
- **禁止“盲目自信”**：不要只写代码不运行。必须编写或执行测试用例。
- **MCP 数据库交互**：如果项目配置了数据库 MCP 服务器，必须调用 MCP 工具插入模拟测试数据，并验证数据流的完整性（如：创建用户 -> 生成 Token -> 验证权限）。
- **第三方依赖处理**：如果功能依赖外部服务（如 Redis、S3、第三方 API），如果无法 mock，必须主动暂停并提醒用户：“检测到需要连接 [服务名] 进行测试，请确认环境已配置，或提供测试凭证。”

### 1.2 测试报告生成
测试完成后，必须在 `docs/omnidev-state/05-test-report.md` 中生成测试报告，包含：
- **测试范围**：覆盖了哪些功能点。
- **测试数据**：使用了哪些模拟数据（脱敏）。
- **测试结果**：成功/失败的用例。
- **功能边界与已知限制**：明确指出当前实现不支持的边缘场景（例如：“当前并发测试仅支持 100 QPS，未做更高压测”）。

## 2. 发布清单生成 (Deployment Docs)

在测试报告确认无误后，必须生成需求上线必备的相关配置文档，存放在 `docs/omnidev-state/06-release-notes.md`。

该文档必须包含：
1. **环境需求**：新增的依赖包、要求的 Node/Go/Python 版本等。
2. **配置变更**：
   - 新增或修改的环境变量（`.env` 变更）。
   - 必须配置的第三方服务密钥。
3. **数据库变更**：
   - 是否需要执行新的 SQL 迁移脚本？
   - 迁移脚本的路径或内容。
4. **部署步骤**：简要的上线操作指南（如：`pnpm install && pnpm build` 或 `docker compose up -d`）。

## 3. 归档与清理 (Archive)
当用户确认发布清单后，提示用户：“🎉 **需求开发与验证已全部完成！** 您可以通过提交代码来结束本次工作流。是否需要我帮您生成 Git Commit Message？”
