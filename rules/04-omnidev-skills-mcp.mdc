---
description: OmniDev Skills 与 MCP 整合规范，指导 AI 如何利用主流开源框架的预置技能和 MCP 服务器。
globs: *
---
# OmniDev Skills & MCP Integration (技能与 MCP 整合)

为了“集众之所长”，OmniDev Kit 整合了来自 `everything-claude-code`、`SkillKit` 等开源生态的优秀实践，并强制 AI 在开发过程中主动利用 MCP (Model Context Protocol) 扩展能力。

## 1. 预置开发技能 (Built-in Skills)

在执行开发任务时，你必须默认启用以下最佳实践（无需用户额外提示）：

### 1.1 安全与防御 (AgentShield)
- **依赖检查**：在引入任何第三方包（npm/go mod/pip）之前，必须检查其是否为已知的主流库，防止依赖混淆攻击。
- **密钥脱敏**：在输出日志、测试报告（`05-test-report.md`）或发布清单时，绝对禁止明文打印任何 Token、密码或 DSN。必须使用 `***` 或 `REDACTED` 替换。

### 1.2 性能与 Token 优化
- **精准阅读**：不要盲目读取整个大型文件。使用 `Grep` 或 `SemanticSearch` 定位具体函数，或使用 `Read` 的 `offset/limit` 参数只读取所需片段。
- **最小化编辑**：修改文件时，优先使用 `StrReplace` 进行精准替换，而不是重写整个文件。

### 1.3 框架特定最佳实践
- **Go / Gin / GORM**：强制使用依赖注入；数据库操作必须检查 `err`；禁止在 Handler 层写复杂的业务逻辑，必须下沉到 Service 层。
- **React / Next.js**：默认启用严格模式；优先使用 Server Components；状态管理避免不必要的 re-render。

## 2. MCP (Model Context Protocol) 协同

MCP 是 AI 连接外部世界的桥梁。在开发和测试阶段（Phase 3 & 4），你必须主动寻找并使用可用的 MCP 服务器。

### 2.1 数据库 MCP (如 MySQL/Postgres MCP)
- **用途**：在测试阶段（Phase 4），不要仅仅“假设代码能跑”。如果检测到工作区配置了数据库 MCP：
  1. 使用 MCP 执行 SQL 查询，验证表结构是否正确创建。
  2. 插入模拟数据，触发你的业务代码。
  3. 再次查询数据库，验证数据状态流转是否符合预期。

### 2.2 浏览器自动化 MCP (如 Playwright/Puppeteer MCP)
- **用途**：对于前端或全栈需求，在测试阶段：
  1. 启动本地服务。
  2. 调用浏览器 MCP 访问 `http://localhost:3000`。
  3. 执行点击、表单填写等端到端 (E2E) 测试。
  4. 截取屏幕快照或提取 DOM 树，验证 UI 渲染是否正确。

### 2.3 发现与调用规则
- 在开始任何复杂任务前，使用 `CallMcpTool` 或查看 `.cursor/mcp.json` 检查当前环境有哪些可用的 MCP 服务器。
- 如果发现缺失关键的 MCP（例如需要测前端却没有 Playwright MCP），请在 `03-progress.md` 中记录，并**主动建议用户安装**（例如：“建议安装 Playwright MCP 以完成自动化 UI 测试”）。
